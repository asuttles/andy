// Find the Pythagorean triplet (a < b < c),
//   for which a^2 + b^2 = c^2,
//   and a + b + c = 1000.

program int

int a, b, c;

begin
   for a := 1; a < 1000; a := a + 1; do
      for b := a + 1; b < 1000; b := b + 1; do
         begin
	    c := 1000 - a - b;
	    if a * a + b * b = c * c then
	       ret a * b * c;
	 end
end.	 